# Cluster node shard-c — see cluster.conf and rocket-mem.toml (shard-a) for context.
#
# cluster.conf advertises this node as numericlabs.lxd:16381, which must therefore be the
# TLS RESP listener: a client following a MOVED redirect here has to reach the same kind of
# port on every node, and shard-a advertises its TLS listener. The plaintext ports below stay
# available for local use, but nothing in the cluster topology points at them.

addr = "numericlabs.lxd:6381"
rmp_addr = "numericlabs.lxd:7381"
metrics_addr = "numericlabs.lxd:9123"

tls_resp_addr = "numericlabs.lxd:16381"
tls_rmp_addr = "numericlabs.lxd:17381"
tls_cert_path = "/home/numericlabs/data/tls/server.crt"
tls_key_path = "/home/numericlabs/data/tls/server.key"

aof_path = "./appendonly-shard-c.aof"
snapshot_path = "./dump-shard-c.snapshot"

slowlog_threshold_micros = 0

cluster_config = "/home/numericlabs/data/rocket/rocket-mem/cluster.conf"
cluster_node_id = "shard-c"

# Must match shard-a's ACL users: a client redirected here by MOVED re-authenticates
# against this node, not shard-a.
[[acl.users]]
username = "app"
password = "changeme"
enabled = true
rules = ["allcommands", "allkeys"]
